{
    "name": "condoplus-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "cd server && npm install && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}